SUBGRAPH(test)
        DATUM(x);
        DATUM(y);
        INPUT(x);
        INPUT(y);
        DATUM(z);
	      OPERATOR(z,op_PLUS,x,y);
	      OUTPUT(z);

SUBGRAPH(main)
        CONST(a,7);
        CONST(b,8);
        DATUM(c);
        EXPAND(test,MAP_IN(x,a);MAP_IN(y,b);MAP_OUT(z,c););
        OUTPUT(c);
